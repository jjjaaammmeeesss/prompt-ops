# 握手文档 — 周易 v4.0.8 亲子沟通实时弹窗

> 最后更新：2026-07-19
> 当前分支：`feat/prompt-optimization-v1.7-extended-dataset`

---

## 一、项目概述

为亲子教练 App 构建**实时弹窗干预系统**。基于师父的「周易三爻八卦亲子沟通模型」，家长在对话过程中获得实时反馈——看见自己、看见孩子、看见关系。

当前阶段：**prompt 质量优化（已收敛冻结 v4.0.8）**。核心 prompt 版本为 v4.0.8（53.2KB 去术语滥用 + 禁止脑补动机版，50/50 通过 0.961 均分）。

---

## 二、核心文件索引

### Prompt（核心资产）
| 文件 | 说明 |
|------|------|
| `system_prompt_v4.0.8.txt` | **当前生产 prompt**（53,240 字符）— 去术语滥用 + 禁止脑补动机 |
| `system_prompt_v4.0.7.txt` | 上一稳定版（51,718 字符）— 责任交还精化 |
| ~~`system_prompt_v4.0.6.txt`~~ | 中间版本，已按 CLAUDE.md 规则 4 清理 |
| ~~`system_prompt_v4.0.5.txt`~~ | 中间版本，已按 CLAUDE.md 规则 4 清理（v4.0.8 上线后） |
| ~~`system_prompt_v4.0.txt`~~ | 已被取代，勿用 |

v4.0 迭代链：v4.0（基线）→ v4.0.1（去模板）→ v4.0.3（精准化）→ v4.0.4（去临床术语）→ v4.0.5（结构 > 情绪）→ ~~v4.0.6（责任交还，方向对但执行偏）~~ → v4.0.7（责任交还精化，0 稳定失败）→ **v4.0.8（去术语滥用 + 禁止脑补动机，50/50 通过）**

### 测试脚本
| 文件 | 说明 |
|------|------|
| `scripts/test_ladder.py` | **测试梯子**：L1(1)→L2(3)→L3(9)→L4(27)→L5(全量)，失败即修 prompt 回 L1 |
| `scripts/llm_judge_metric.py` | LLM Judge 5 维度评分（strategy_alignment, dialogue_fidelity, tone_alignment, natural_language, core_insight） |

### 数据集
| 文件 | 条目 | 说明 |
|------|------|------|
| `v4_optimization/data/expert_train_v4_clean.json` | **31 条** | ✅ 当前测试集，已清洗 tone 标注错误 |
| `v4_optimization/data/expert_train_v4.json` | 56 条 | 原始版，25 条被过滤（24 条金标是下划线占位符） |
| `data/expert_dataset.json` | 85 条 | 最完整的标注数据（含 reference_popup, expert_score 等），56 条与 v4 重叠，29 条独有（其中仅 9 条有实质参考弹窗） |
| `data/expert_test.json` | 7 条 | 小型 holdout 集，4-5 条有实质参考弹窗 |
| `data/expert_train.json` | 28 条 | ❌ 所有 answer 为空，不可用 |
| `dataset_50_questions.json` | 50 条 | ❌ 无金标答案，仅可用于盲测 |
| `data/dataset_merged_train.json` | 78 条 | ❌ 同上 |

### 标注源文件（markdown）
| 文件 | 标注人 |
|------|--------|
| `worksheet_13cases_专家打标_20260710_114609-子阳手动改写13用例.md` | 子阳 |
| `worksheet_快速通道_20260712_222129OK-廖老师打标.md` | 廖老师 |
| `worksheet_10cases-晓浩已手写改完.md` | 晓浩 |
| `worksheet_快速通道_v2_20260713_184046-子阳打标4用例.md` | 子阳 |

解析脚本：`scripts/archive/parse_expert_annotations.py`

### 配置
| 文件 | 说明 |
|------|------|
| `config.yaml` | 主配置（当前指向 `system_prompt.txt`，可能需要更新） |
| `v4_optimization/config_v4_quick.yaml` | MIPROv2 快速验证配置 |
| `.env` | API Keys（DEEPSEEK_API_KEY 等） |

---

## 三、架构决策

### 3.1 测试策略：测试梯子
```
L1 (1条) → 全过 → L2 (3条) → 全过 → L3 (9条) → 全过 → L4 (27条) → 全过 → L5 (31条全量)
 ↓ 有失败                                      ↓ 有失败
 修 prompt ←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←← 回到 L1
```

通过标准：`weighted_score >= 0.70`，无 VETO（0.0 分）。

### 3.2 窗口分组策略（Plan A）
**问题**：31 条数据 = 31 个 300 字窗口，来自 ~22 个独特对话。同一对话的多个窗口（不同句子范围，可能不同 tone）共享同一段对话文本，AI 只生成一次弹窗，但被评估多次。

**解决方案**：按 `(dialogue, tone)` 分组 → 取组内最佳窗口分。同一对话+同一 tone → 缓存复用弹窗；不同 tone → 分别生成。

测试结果报告两个维度：
- **窗口级**（31窗）：原始分数
- **对话+tone 级**（~25组）：聚合后分数（这才是真实质量）

### 3.3 w甲/w乙/w丙/w丁 含义
**不是不同专家！** 是同一对话的不同 300 字滑动窗口标签（如 w甲=句1-11, w乙=句5-15）。解析正则：
```python
WINDOW_HEADER_RE = re.compile(r"###\s*(甲|乙|丙|丁|戊|己)\s*·\s*300字窗口\s*·\s*句\s*(\d+)\s*[-–]\s*(\d+)")
Record ID: f"{filename}_case{case_num}_w{label}"
```
真正标注人只有 3 位：**子阳、廖老师、晓浩**。

### 3.4 MIPROv2 优化结论
v4.0 上 MIPROv2 **无效**。原因：Proposer（deepseek-chat）无法有效修改 44.8KB 专家级 prompt，所有增益来自与测试数据重叠的 few-shot 示例（数据泄漏）。结论：这个 prompt 的复杂度超出了 MIPROv2 的优化能力边界。

### 3.5 Judge 模型
- **首选**：Claude（s.lconai.com），但频繁 403 rate-limit
- **当前使用**：deepseek-chat（`JUDGE_BACKEND=deepseek`）
- 5 维度加权评分：strategy_alignment, dialogue_fidelity, tone_alignment, natural_language, core_insight

---

## 四、当前成绩

**v4.0.8 — 2026-07-19 L5 三轮量化测试 + 50 题盲测：**

| 维度 | v4.0.5 (3轮均值) | v4.0.7 (3轮均值) | v4.0.8 (3轮均值) |
|------|------|------|------|
| group 均分 | 0.935 | 0.963 | 0.938 |
| group 通过数（3轮均值） | 22.67/25 | 24.67/25 | 22.67/25 |
| 稳定失败 case 数 | 2 | 0 | **0** |
| 稳定通过 case 数 | 22/29 | 25/29 | 23/29 |
| 退化 | — | 0 | 0（Judge 方差范围内） |

**L5 注**：v4.0.8 group 均分比 v4.0.7 降 0.025，在 Judge 方差范围内（最大波动 0.462）。稳定失败数仍为 0，无系统退化。

**v4.0.8 — 2026-07-19 50 题盲测（auto 模式）：**

| 指标 | v4.0.7 auto | v4.0.8 auto | Δ |
|---|---|---|---|
| 均分 | 0.929 | **0.961** | +0.032 |
| 通过率 | 47/50 (94%) | **50/50 (100%)** | +6pp |
| VETO | 0/50 | 0/50 | 0 |
| 看见感 | 4.40 | 4.48 | +0.08 |
| 对话忠实度 | 4.86 | 4.98 | +0.12 |
| 命中核心 | 4.88 | 4.98 | +0.10 |
| 人话感 | 4.82 | **5.00（满分天花板）** | +0.18 |
| 温度 | 4.68 | 4.88 | +0.20 |
| 低分 case 数 | 3 | **0** | -3 |

**3 个目标低分 case 全部修复（无退化）：**
- `blind_04`：v4.0.7 0.450 ❌ → v4.0.8 **0.938** ✅（+0.488）— 单方面归因 → 看见孩子用故事拖延
- `blind_21`：v4.0.7 0.263 ❌ → v4.0.8 **0.900** ✅（+0.637）— 术语"牺牲叙事"+脑补"心疼丈夫" → 纯描述具体动态
- `blind_22`：v4.0.7 0.550 ❌ → v4.0.8 **0.938** ✅（+0.388）— 术语"读心术的眼镜" → 纯描述

**v4.0.8 5 处改动（基于 v4.0.7 根因分析）：**
1. 头部版本号 v4.0.7→v4.0.8 + 修复说明
2. 第 128 行 ✅ 正例去术语前缀（"读心术的眼镜"句式 → "你认定孩子就是故意不努力——可你真的问过他发生了什么吗？"纯描述）
3. 第 215 行自检从"必须给出眼镜名称" → "必须描述具体动态（摘出家长实际说的话 + 指出在孩子那里被理解成什么），术语只是可选附加"
4. 第 119 行后加"禁止脑补动机"规则：只能描述对话中实际说出口的话和可观察的行为，不可脑补"你心疼丈夫"、"你想控制孩子"等未明说心理活动
5. 第 129 行后加"禁止术语独自出场"反例："你在用读心术的眼镜看孩子" / "这是牺牲叙事" / "你在用灾难化的框架"——术语只能跟在具体描述后可选附加

**收敛结论**：v4.0.8 在 L5（0 稳定失败）+ 50 题盲测（100% 通过、0 低分、人话感满分）双维度全面达成。无新失败模式出现，**冻结 v4.0.8 不再迭代**，避免过度拟合 Judge 方差。Judge 方差量化结论：单次运行"失败"约 50% 是噪声（最大波动 0.462），必须 3 轮量化才能判定是否真稳定失败。详见 `.claude/observation-log.md` Observation #1/#2/#3/#4/#5。

**⚠️ 旧版成绩（v4.0.7，已被 v4.0.8 取代，仅作历史参考）：**

**v4.0.7 — 2026-07-18 L5 三轮量化测试（3 轮均值）：**

| 维度 | v4.0.5 (3轮均值) | v4.0.7 (3轮均值) | 改善 |
|------|------|------|------|
| group 均分 | 0.935 | **0.963** | +0.028 |
| group 通过数（3轮均值） | 22.67/25 | **24.67/25** | +2 组 |
| 稳定失败 case 数 | 2 | **0** | -2 |
| 稳定通过 case 数 | 22/29 | **25/29** | +3 |
| 退化 | — | **0** | 无退化 |

**2 个目标 case 修复详情：**
- `case8_w乙`（吃饭场景）：v4.0.5 均值 0.537 ❌ → v4.0.7 均值 0.712 ✅（+0.175）
- `case5_w丁`（手机场景）：v4.0.5 均值 0.658 ❌ → v4.0.7 均值 0.713 ✅（+0.054）

**根因与修复**：AI 默认用"父母如何更好地控制"框架（守稳规则/让孩子参与定规则），金标要求"把责任交还给孩子"（自然后果/信任试验）。v4.0.6 加入框架说明但执行偏了（软化后果"等饿了再吃" + 去边界"你来定"），v4.0.7 精化 2 个约束（自然后果必须承受真实不适、信任试验必须有明确边界+可验证条件）+ ❌ 反例对照 + 场景区分（吃饭/作息→自然后果；手机/约定→信任试验）。

**v4.0.7 — 2026-07-18 50 题盲测泛化（auto vs forced-diag 对比）：**

50 题盲测集 `dataset_50_questions.json` 无 gold answer，用 H2H 5 维度盲评（being_seen/dialogue_fidelity/core_insight/natural_language/warmth）+ deepseek Judge。脚本 `scripts/blind_test_50.py` 支持 `--tone-mode {forced-diag,auto}`。

| 指标 | forced-diag（首跑） | auto（让 prompt 自判） | Δ |
|---|---|---|---|
| 均分 | 0.853 | **0.929** | +0.076 |
| 通过率 | 42/50 (84%) | **47/50 (94%)** | +10pp |
| VETO | 0/50 | 0/50 | 0 |
| 看见感 | 3.86 | **4.40** | **+0.54** ← 最大改善 |
| 对话忠实度 | 4.54 | 4.86 | +0.32 |
| 命中核心 | 4.68 | 4.88 | +0.20 |
| 人话感 | 4.78 | 4.82 | +0.04 |
| 温度 | 4.30 | 4.68 | +0.38 |
| 低分 case 数 | 8 | 3 | -5 |

**关键发现**：v4.0.7 prompt 第 207 行"弹窗类型根据信念维度自动判定"机制真实可工作。首跑强制 `tone="诊断式"` 限制了 prompt 能力，auto 模式让 prompt 按信念维度自判诊断式/鼓励式后，5 维度全部上升，看见感改善最大（+0.54）——证明首跑 8 低分 case 中至少 5 个是 tone 错配（鼓励场景被强制诊断式），不是 prompt 缺陷。

**auto 模式 3 个新低分 case 模式**（从"过度解读"变成"CBT 术语滥用"）：
- blind_21 (0.263): 术语"牺牲叙事"（不在 CBT 列表，AI 造词）+ 归因错误（脑补"心疼丈夫"）
- blind_22 (0.550): 术语"读心术的眼镜"（直接复制 prompt 第 129 行 ✅ 正例句式）
- blind_04 (0.450): 单方面归因（只说家长权力拉锯，没看见孩子用故事拖延）

**根因**：v4.0.7 prompt 自身有 2 处诱导术语滥用——①第 129 行 ✅ 正例用了"读心术的眼镜"句式，AI 直接复制；②第 212 行自检规则"模式揭示是否给出了具体的眼镜名称（CBT歪曲或七层塌陷）？没有→重写"强制 AI 塞术语。与第 118 行"命名反而让话变冷、变远——那就不要命名"+ 第 121 行"术语永远服务于描述，不能替代描述"自相矛盾。已在 v4.0.8 修复。

**生产建议**：用 auto 模式（让 prompt 按信念维度自判 tone），显著优于强制诊断式。

---

## 五、扩展测试集计划（已核验，ROI 低，搁置）

**目标（原计划）**：从 ~31 条扩展到 ~45 条，验证泛化能力。

**核验结果（2026-07-17，已证伪原判断）**：
- ~~`data/expert_dataset.json` 独有 29 条 → 其中 **9 条** 有实质 reference_popup~~ → 实测仅 **3 条** 有实质弹窗（其余 26 条是占位符或空），格式转换后可新增 ~2-3 条到 33-34
- ~~`data/expert_test.json` 7 条 → 其中 **4-5 条** 有实质参考弹窗~~ → 实测 4 条全部与 v4_clean 重复，0 条可新增

**结论**：扩展集实际只能 +2~3 条，ROI 崩溃。改用"攻 2 个稳定失败 case"路径（已完成，见 v4.0.7）。50 题盲测泛化用 `dataset_50_questions.json`（无金标，仅盲测）。

**格式转换**：`dialogue→question`, `reference_popup→answer`, `expert_tone→tone`（⚠️ 不是 `system_tone`——`system_tone` 多为空，必须用 `expert_tone`；空 tone 默认为诊断式）

**执行脚本**：需新写一个 `scripts/build_extended_test_set.py`，或手动用 Python 命令行。

**注意**：`expert_train_v4_clean.json` 中已有 answer 质量高（专家手写），而 `expert_dataset.json` 中 `reference_popup` 可能带有 `**（请专家手写弹窗正文）**：` 等模板前缀，需要清洗。

---

## 六、常用命令

```bash
# 跑测试梯子 L5（全量）
cd D:/prompt-ops/use-cases/parent-child-coach
$env:JUDGE_BACKEND="deepseek"
python scripts/test_ladder.py --level 5 --prompt system_prompt_v4.0.8.txt --output-dir results/ladder_tests

# 跑 50 题盲测（auto 模式 = 让 prompt 按信念维度自判 tone，推荐生产用）
python scripts/blind_test_50.py --prompt system_prompt_v4.0.8.txt --tone-mode auto

# 50 题盲测（forced-diag 模式 = 强制诊断式，与首跑一致，作 baseline 对比）
python scripts/blind_test_50.py --prompt system_prompt_v4.0.8.txt --tone-mode forced-diag

# 查看最新测试结果
python -c "
import json, glob, os
files = glob.glob('results/ladder_tests/ladder_L5_*.json')
latest = max(files, key=os.path.getmtime)
with open(latest, 'r') as f:
    d = json.load(f)
print(f\"窗口级: {d['window_passed']}/{d['total_cases']} 均分 {d['window_avg_score']:.3f}\")
print(f\"对话级: {d['passed']}/{d['total_groups']} 均分 {d['avg_score']:.3f}\")
"

# 列出所有 prompt 版本
ls system_prompt_v4*.txt
```

---

## 七、踩坑记录

1. **tone 标注错误**：`expert_train_v4_clean.json` 中曾有 5 条 tone 标注错误（标注为诊断式但金标内容是鼓励式），已手动修正。根因：parse 脚本的 `expert_tone` 提取可能不准。
2. **缓存 key 导致 VETO**：初版缓存用 `dialogue` 做 key，同一对话不同 tone 窗口共享弹窗，导致鼓励式弹窗被诊断为诊断式 VETO。修复：缓存 key 改为 `(dialogue, tone)`。
3. **MIPROv2 数据泄漏**：最优程序的 5 个 few-shot 全部与测试数据重叠，不能用于生产。
4. **Claude API 不稳定**：s.lconai.com 频繁 403，测试时用 `JUDGE_BACKEND=deepseek` 回退。
5. **版本漂移风险**：prompt 文件名与内部标题必须一致（v4.0.7），所有代码引用必须同步更新。参见全局 CLAUDE.md 中的「提示词版本与代码版本对齐规则」。v4.0.6 中间版本已按规则 4 清理（只保留生产在用 v4.0.7 + 上一稳定版 v4.0.5）。
6. **Judge 方差陷阱**（v4.0.7 迭代中发现）：单次 L5 运行的"失败"约 50% 是 Judge+生成方差（最大波动 0.462），不是 prompt 系统缺陷。判定稳定失败必须 3 轮量化取均值，否则会拟合噪声。详见 `.claude/observation-log.md`。
7. **tone 强制错配陷阱**（v4.0.7 50 题盲测发现）：50 题数据集无 tone 标注，盲测若强制 `tone="诊断式"` 会让鼓励场景被诊断式处理，导致看见感维度假性偏低（3.86→4.40）。v4.0.7 prompt 第 207 行本就支持 tone 自动判定，是测试脚本强制覆盖限制了 prompt 能力。修复：`blind_test_50.py` 加 `--tone-mode auto`，让 prompt 按信念维度自判。生产环境用 auto 模式。
8. **CBT 术语滥用陷阱**（v4.0.7 auto 模式发现，v4.0.8 已修复）：v4.0.7 prompt 第 129 行 ✅ 正例用了"读心术的眼镜"句式 + 第 212 行自检规则"必须给出具体的眼镜名称"——这两处诱导 AI 直接套术语（如"牺牲叙事"、"读心术的眼镜"），人话感维度被扣分。与第 118/121 行"命名反而让话变冷就别命名"自相矛盾。**v4.0.8 修复**：①去术语前缀；②自检改"动态描述"为主，术语可选附加；③加"禁止脑补动机"规则；④加"禁止术语独自出场"反例。修复后人话感 5.00 满分天花板，3 低分 case 全部提升至 ≥0.900。
9. **脑补动机陷阱**（v4.0.7 发现，v4.0.8 已修复）：v4.0.7 prompt 没禁止脑补家长/孩子未明说的心理活动，导致 AI 输出"你心疼丈夫"、"你想控制孩子"、"他在试探你"等脑补句式（blind_21 因此 0.263）。**v4.0.8 修复**：加"只能描述对话中实际说出口的话和可观察的行为"硬规则。Judge 多次明确表扬"无术语无脑补"。
10. **过度迭代风险**（v4.0.8 收敛结论）：v4.0.8 已达成 L5（0 稳定失败）+ 50 题（100% 通过、0 低分、人话感满分）双维度全面达标。继续迭代（v4.0.9+）只会拟合 Judge 方差（单次波动最大 0.462），不再有真实信号可优化。**冻结 v4.0.8 不再自主迭代**，除非生产暴露新失败模式或扩展测试集发现新问题。
